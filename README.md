# NutriApp

Aplicación web (frontend) para registro diario de comidas, recetas propias y carga de valores nutricionales.

## Ejecutar

Como es una SPA estática, podés abrir `index.html` directamente o correr un servidor local:

```bash
python3 -m http.server 4173
```

Luego abrir `http://localhost:4173`.
